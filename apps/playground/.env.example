# Leave empty for local Vite development (the proxy targets localhost:3000).
# For Cloudflare Pages, set this to the public Render API origin, for example:
# VITE_API_ORIGIN=https://centrum-api.onrender.com
VITE_API_ORIGIN=

